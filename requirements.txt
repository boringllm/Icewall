# Core runtime (mock provider works with just these — no API keys).
tree-sitter>=0.23
tree-sitter-language-pack>=0.7
pydantic>=2.6
typer>=0.12
rich>=13.7
pyyaml>=6.0

# Optional real providers (install as needed):
# anthropic>=0.40
# openai>=1.40
